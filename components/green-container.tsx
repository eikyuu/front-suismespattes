function GreenContainer({ children }: { children: React.ReactNode }) {
  return (
    <section className="mt-10 h-auto rounded-md bg-primary p-5">
      {children}
    </section>
  )
}

export default GreenContainer
